import express from "express";import {getAllEmployee} from "../repositories/employeeRepository";const employeeRouter = express.Router();import {Request, Response} from "express";employeeRouter.get('/', async (req: Request, res: Response) => {	try {		const employee = await getAllEmployee();		res.status(200).json(employee);	} catch (error) {		console.error('Error fetching employee:', error);		res.status(500).json({error: 'Internal Server Error'});	}});//TODO : Login route (store user id in front)//TODO : Register route/*employeeRouter.get('/:id', async (req: Request, res: Response) => {	try {		const employee = await getEmployeeById(req.params.id);		if (!employee) {			return res.status(404).json({ error: 'employee not found' });		}		res.status(200).json(employee);	} catch (error) {		console.error('Error fetching employee by ID:', error);		res.status(500).json({ error: 'Internal Server Error' });	}});employeeRouter.post('/', async (req: Request, res: Response) => {	try {		const newEmployee = await createEmployee(req.body);		res.status(201).json(newEmployee);	} catch (error) {		console.error('Error creating employee:', error);		res.status(500).json({error: 'Internal Server Error'});	}}); *///TODO : DeleteByUserId route//TODO : PatchByUserId routeexport default employeeRouter;